# Sanitized HID report fixtures

Fixtures contain no serial numbers, Bluetooth addresses, or device paths. Each file records the
model, connection, report type, expected battery state, and whether it is specification-derived.
Real-hardware captures may be added only after identifiers are removed.
