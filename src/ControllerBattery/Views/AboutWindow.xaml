<Window x:Class="ControllerBattery.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:behaviors="clr-namespace:ControllerBattery.Behaviors"
        Title="About Controller Battery" Icon="/Assets/controller-battery.ico"
        Width="460" Height="520" MinHeight="380" ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner" Background="#15141D" FontFamily="Segoe UI"
        WindowStyle="None" UseLayoutRounding="True" SnapsToDevicePixels="True">
  <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="46" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0" UseAeroCaptionButtons="False"/></shell:WindowChrome.WindowChrome>
  <Window.Resources>
    <Style x:Key="TitleBarButton" TargetType="Button"><Setter Property="Width" Value="48"/><Setter Property="Height" Value="45"/><Setter Property="Padding" Value="0"/><Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="#C8C4D3"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="Surface" Background="{TemplateBinding Background}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#C42B3A"/><Setter Property="Foreground" Value="White"/></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="#A52230"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
    <Style TargetType="Button" BasedOn="{StaticResource ModernButton}"/>
    <Style x:Key="ScrollBarPageButton" TargetType="RepeatButton"><Setter Property="Focusable" Value="False"/><Setter Property="IsTabStop" Value="False"/><Setter Property="Opacity" Value="0"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate></Setter.Value></Setter></Style>
    <Style TargetType="ScrollBar"><Setter Property="Width" Value="8"/><Setter Property="Margin" Value="8,2,0,2"/><Setter Property="Background" Value="Transparent"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ScrollBar"><Grid Background="Transparent"><Border Width="4" Background="#242230" CornerRadius="2" HorizontalAlignment="Center"/><Track x:Name="PART_Track" IsDirectionReversed="True"><Track.DecreaseRepeatButton><RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand"/></Track.DecreaseRepeatButton><Track.Thumb><Thumb MinHeight="32"><Thumb.Template><ControlTemplate TargetType="Thumb"><Border x:Name="ThumbBody" Width="6" Background="#625D78" CornerRadius="3" HorizontalAlignment="Center"/><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ThumbBody" Property="Background" Value="#8B7CF6"/></Trigger><Trigger Property="IsDragging" Value="True"><Setter TargetName="ThumbBody" Property="Background" Value="#A99CF8"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Thumb.Template></Thumb></Track.Thumb><Track.IncreaseRepeatButton><RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand"/></Track.IncreaseRepeatButton></Track></Grid></ControlTemplate></Setter.Value></Setter></Style>
  </Window.Resources>
  <Grid x:Name="AboutRoot" Opacity="0">
    <Grid.RowDefinitions><RowDefinition Height="46"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Border Background="#191823" BorderBrush="#2A2835" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
      <Grid Margin="10,0,0,0"><StackPanel Orientation="Horizontal" VerticalAlignment="Center"><Image Source="/Assets/controller-icon-titlebar.png" Width="42" Height="38" Stretch="Uniform"/><TextBlock Text="About" Foreground="#F0EDF7" FontSize="14" FontWeight="SemiBold" Margin="9,0,0,0" VerticalAlignment="Center"/></StackPanel><Button Style="{StaticResource TitleBarButton}" HorizontalAlignment="Right" Click="CloseButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Close"><Path Data="M 2,2 L 10,10 M 10,2 L 2,10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2"/></Button></Grid>
    </Border>
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" CanContentScroll="False" PanningMode="VerticalOnly" behaviors:SmoothScrollBehavior.IsEnabled="True" Margin="28,28,0,0" Padding="0,0,18,0">
      <StackPanel>
        <Border Width="82" Height="72" CornerRadius="20" Background="#27243A" HorizontalAlignment="Left"><Image Source="/Assets/controller-icon-titlebar.png" Width="66" Height="56" Stretch="Uniform"/></Border>
        <TextBlock Text="Controller Battery" Foreground="#F4F2FF" FontSize="28" FontWeight="SemiBold" Margin="0,18,0,0"/>
        <TextBlock Text="A unified Windows dashboard for controller battery levels, profiles, notifications, and quick in-game checks." Foreground="#9995AA" FontSize="13" LineHeight="20" TextWrapping="Wrap" Margin="0,7,0,0"/>

        <TextBlock Text="BUILD" Foreground="#8B7CF6" FontSize="11" FontWeight="Bold" Margin="0,26,0,9"/>
        <Border Background="#22202D" CornerRadius="12" Padding="18">
          <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Application version" Foreground="#F4F2FF" FontSize="14" FontWeight="SemiBold"/><Border Grid.Column="1" Background="#302A50" CornerRadius="8" Padding="10,5"><TextBlock x:Name="BuildVersionText" Foreground="#CFC7FF" FontFamily="Consolas" FontSize="12"/></Border></Grid>
        </Border>

        <TextBlock Text="CONTROLLER SUPPORT" Foreground="#8B7CF6" FontSize="11" FontWeight="Bold" Margin="0,24,0,9"/>
        <Border Background="#22202D" CornerRadius="12" Padding="18">
          <StackPanel><TextBlock Text="Supported providers" Foreground="#F4F2FF" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="Xbox-compatible controllers through XInput, DualSense and DualSense Edge, official Switch Pro controllers, and native 8BitDo HID devices." Foreground="#9995AA" FontSize="12" LineHeight="19" TextWrapping="Wrap" Margin="0,6,0,0"/><TextBlock Text="Battery detail and available actions depend on what each connection protocol exposes." Foreground="#777386" FontSize="11" LineHeight="17" TextWrapping="Wrap" Margin="0,10,0,0"/></StackPanel>
        </Border>

        <TextBlock Text="NOTICE" Foreground="#8B7CF6" FontSize="11" FontWeight="Bold" Margin="0,24,0,9"/>
        <Border Background="#22202D" CornerRadius="12" Padding="18" Margin="0,0,0,12"><TextBlock Text="Controller Battery is an independent project and is not affiliated with Sony, Microsoft, Nintendo, 8BitDo, or other controller manufacturers. Product names and trademarks belong to their respective owners." Foreground="#9995AA" FontSize="12" LineHeight="19" TextWrapping="Wrap"/></Border>
      </StackPanel>
    </ScrollViewer>
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="28,16,28,28"><Button Content="Close" Click="CloseButton_Click" Background="#6D5DD3"/></StackPanel>
    <Border Grid.RowSpan="3" BorderBrush="#6C6482" BorderThickness="1" CornerRadius="11" IsHitTestVisible="False" Panel.ZIndex="100"/>
  </Grid>
</Window>
